// import { computerMatchImpact } from "@proqueue/analytics"

// export async function processMatch (matchId: string) {
//     const rawMatch = await riotApi.getMatch(matchId)
//     const parsed = parseMatch(rawMatch)
//     const impact = computeMatchImpact(parsed)
    
//     await db.saveMatch(parsed, impact)
//     return impact
// }